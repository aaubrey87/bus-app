<p id="notice"><%= notice %></p>

<h1>Your Escape Bus...</h1>

<h3 class="text-muted">Your current location</h3>

<p class="text-muted">
  <%= @location.full_street_address %>
  <%= link_to fa_icon('pencil', text: 'Edit'), edit_location_path(@location) %>
</p>

<h3>The closet buses are...</h3>
<% @buses.each do |bus|  %>
<div class="well">
  <strong>Route: </strong><%= bus["ROUTE"] %> <%= bus["DIRECTION"] %><br>
  <strong>Bus Number: </strong><%= bus["VEHICLE"] %><br>
  <strong>Next Stop: </strong><%= bus["TIMEPOINT"] %><br>
</div>
<% end %>

<% if @buses.empty? %>
<p>
  Not really that close. Time to walk, bike, frolic, or <%= link_to 'edit_location_path',
  edit_location_path(@location)%>
</p>
<% end %> 